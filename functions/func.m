function output = func(x, y)
%func - This computes a scalar using cosine, sine.
%
% Syntax: output = func(x, y,)
%
% Just a random function to test he Github Actions
% testing framework for matlab functions.


output = 1-(sin(x).^2 + cos(y).^2);
    
end